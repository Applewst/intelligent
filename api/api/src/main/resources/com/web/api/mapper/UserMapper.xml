<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.web.api.mapper.UserMapper">
    <insert id="addUser">
        insert into
            user (username, password, salt, identity)
        values
            (#{username}, #{password}, #{salt}, #{identity})
    </insert>

    <update id="modifyUserById">
        update user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
                salt = #{salt},
            </if>
            <if test="identity != null and identity != ''">
                identity = #{identity},
            </if>
        </set>
        where
            id = #{id}
            and is_disabled = 0
    </update>
    <delete id="deleteUserById">
        update user
        set is_disabled = 1
        where id = #{id}
    </delete>

    <select id="findUserByName" resultType="com.web.api.pojo.User">
        select *
        from user
        where username = #{username}
            and is_disabled = 0
    </select>

    <select id="findIdNameIdentityByName" resultType="com.web.api.pojo.JwtData">
        select id, username, identity
        from user
        where username = #{username}
            and is_disabled = 0
    </select>

    <select id="getAllUser" resultType="com.web.api.pojo.User">
        select id, username, identity
        from user
        where
            username like concat('%', #{name}, '%')
            and
            is_disabled = 0
    </select>
    <select id="findUserById" resultType="boolean">
        select exists(select 1 from user where id = #{id} and is_disabled = 0)
    </select>
</mapper>

